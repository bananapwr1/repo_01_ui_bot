# 🚀 Инструкции по деплою UI-Бота на Bothost

## ✅ Исправленные проблемы

Код был исправлен для корректной работы с переменными окружения:

1. **Исправлена инициализация Supabase** - теперь использует `SUPABASE_URL` и публичный `SUPABASE_KEY` (опционально)
2. **Добавлена проверка переменных** - бот не запустится с понятным сообщением об ошибке, если переменные не установлены
3. **Исправлено шифрование** - функция `encrypt_data()` теперь автоматически получает ключ из переменных окружения
4. **Добавлен импорт `Optional`** - исправлена ошибка типизации

## 📋 Переменные окружения для Bothost

Установите следующие переменные окружения в настройках вашего бота на Bothost:

### Обязательные переменные:

| Имя переменной | Значение | Описание |
|---------------|----------|----------|
| `TELEGRAM_BOT_TOKEN` (или `BOT_TOKEN` / `TELEGRAM_BOT_TOKEN_UI`) | `<your_telegram_bot_token>` | Токен Telegram-бота |
| `ENCRYPTION_KEY` | `<your_fernet_key>` | Ключ шифрования (Fernet) |
| `PORT` | `8000` | Порт для API-сервера |
| `SUPABASE_URL` | `https://your-project.supabase.co` | (Опционально) URL Supabase проекта |
| `SUPABASE_KEY` | `<your_supabase_public_key>` | (Опционально) публичный ключ Supabase |
| `ADMIN_USER_ID` | `<your_telegram_user_id>` | (Опционально) root admin id для админ-команд |

### Копируйте эти значения в Bothost:

```bash
# Telegram token (use ONE of these; preferred first):
TELEGRAM_BOT_TOKEN=<your_telegram_bot_token>
# BOT_TOKEN=<your_telegram_bot_token>
# TELEGRAM_BOT_TOKEN_UI=<your_telegram_bot_token>

ENCRYPTION_KEY=<your_fernet_key>
PORT=8000

# (Опционально) Только если используете Supabase для signal_requests:
SUPABASE_URL=https://your-project.supabase.co
SUPABASE_KEY=<your_supabase_public_key>

# (Опционально) Root-доступ к админ-командам:
ADMIN_USER_ID=<your_telegram_user_id>
```

## 🔧 Инструкции по установке на Bothost

### Шаг 1: Подключение репозитория
1. Войдите в Bothost.com
2. Выберите ваш бот `repo_01_ui_bot`
3. Убедитесь, что репозиторий подключен и актуален

### Шаг 2: Настройка переменных окружения
1. Перейдите в раздел "Environment Variables" (Переменные окружения)
2. Добавьте все переменные из таблицы выше
3. **Важно**: Убедитесь, что имена переменных точно совпадают (с учетом регистра)

### Шаг 3: Настройка зависимостей
Убедитесь, что файл `requirements.txt` содержит:
```
python-telegram-bot
python-dotenv
supabase
cryptography
fastapi
uvicorn
```

### Шаг 4: Запуск бота
1. Сохраните все настройки
2. Нажмите кнопку "Rebuild" или "Deploy"
3. Дождитесь завершения сборки
4. Проверьте логи на наличие ошибок

## ✅ Проверка работы

После успешного запуска вы должны увидеть в логах:

```
🚀 Запуск UI-Бота (Telegram + API Server)...
INFO:     Started server process
INFO:     Waiting for application startup.
INFO:     Application startup complete.
INFO:     Uvicorn running on http://0.0.0.0:8000
```

**НЕ ДОЛЖНО БЫТЬ:**
- ❌ `supabase_key is required`
- ❌ `SUPABASE_URL или SUPABASE_KEY не установлены!` (только если вы включили Supabase-функции)
- ❌ `ENCRYPTION_KEY is required`

## 🌐 Получение URL бота

После успешного запуска:

1. Найдите в настройках Bothost раздел с внешним URL вашего бота
2. URL будет примерно таким: `https://your-bot-name.bothost.net`
3. **Сохраните этот URL** - он понадобится для настройки Render (Ядро Анализа)

## 🧪 Тестирование бота

После запуска протестируйте бота в Telegram:

1. Найдите вашего бота в Telegram
2. Отправьте команду `/start`
3. Вы должны получить приветствие: *"👋 Добро пожаловать! Я — Ваш торговый бот..."*

### Доступные команды:
- `/start` - Приветствие и информация
- `/set_po [логин] [пароль]` - Сохранение учетных данных Pocket Option (зашифрованно)
- `/signal` - Запрос сигнала от Ядра Анализа

## 🔍 Отладка проблем

### Если бот не запускается:

1. **Проверьте логи Bothost** - найдите конкретную ошибку
2. **Проверьте переменные окружения** - убедитесь, что все 5 переменных установлены
3. **Проверьте имена переменных** - они должны ТОЧНО совпадать
4. **Перезапустите бот** - иногда помогает простой рестарт

### Если видите ошибку "Module not found":

1. Проверьте `requirements.txt`
2. Сделайте "Rebuild" на Bothost
3. Убедитесь, что в логах видна установка всех зависимостей

## 📝 Что дальше?

После того, как UI-бот успешно запустится и вы получите его URL:

1. **Сообщите URL** для настройки Render (Ядро Анализа)
2. Настроим связь между UI-ботом и Ядром через Supabase
3. Протестируем полный цикл работы системы

---

## ⚠️ Важные замечания

- **Не коммитьте `.env` в репозиторий!** - файл `.env` добавлен в `.gitignore`
- **Храните ключи в безопасности** - никогда не публикуйте их публично
- **Используйте тестовые данные** - для первых тестов не используйте реальные учетные данные Pocket Option
- **Регулярно обновляйте ключи** - меняйте токены и ключи шифрования периодически

---

Создано: 2024-12-11
Версия: 1.0
Статус: Готово к деплою ✅
